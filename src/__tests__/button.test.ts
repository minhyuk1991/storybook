import { sum } from '../aa';

describe('sum ', () => {
  it('sum', () => {
    expect(sum(1, 2)).toBe(3);
  });
});

describe('button ', () => {
  it('button text props', () => {
    expect(sum(1, 2)).toBe(3);
  });
});
